# Issue 6219 Tasks

- T1 (Unit/Conformance): Add failing tests for `is_help_request` and `render_help_text` semantics.
- T2 (Implementation): Restore exported helper functions in `kamn-cli` crate root.
- T3 (Verification): Run `cargo test -p kamn-cli`.
- T4 (Verification): Run `cargo build -p kamn-cli --release`.
- T5 (Closure): Update issue/PR with AC to test mapping and command evidence.

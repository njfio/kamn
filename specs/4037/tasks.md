# Issue #4037 Tasks

- T1 (Tests first): add RED Rust contract tests for checker baseline, fail-closed artifact-marker, docs-parity drift, and budget behavior.
- T2 (Implementation): add deterministic release checker script with required artifact/docs parity validation and stable reason markers.
- T3 (Docs): add release checker marker/command blocks in CI strategy and ops configuration docs.
- T4 (Verification): run targeted checker/docs tests plus fmt/clippy.
- T5 (Process): update issue/PR process logs with AC mapping, RED/GREEN evidence, and shell-surface DoD markers.

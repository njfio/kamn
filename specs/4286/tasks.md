# Tasks — #4286

Status: Implemented

T1
- Implement deterministic `check-policy` mode in shared failover preflight contract module.

T2
- Ensure deterministic fail-closed reason mapping for drift parity, missing markers, and taxonomy drift.

T3
- Validate via targeted script tests and report schema assertions.

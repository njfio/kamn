# Issue #5513 Tasks

- T1 (RED): add failing service API endpoint assertions for cross-store replay metrics lines.
- T2 (Implementation): project cross-store replay taxonomy metadata into `ServiceApiSnapshot` and `/metrics` rendering.
- T3 (GREEN): run targeted `kamn-node` service API endpoint tests.
- T4 (Regression/Verify): run formatting checks.
- T5 (Process): open PR, merge, close issue and milestone.

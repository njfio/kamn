# Tasks — #4243 Implement Replay Taxonomy and Runbook Parity Contract

Status: Reviewed

- T1 (Implementation): add replay taxonomy/runbook marker constants and output fields.
- T2 (Implementation): add policy `--runbook-file` and runbook marker parity validation.
- T3 (Implementation): add deterministic replay taxonomy drift + runbook parity reason mapping.
- T4 (Docs): update deploy/release docs and docs-contract tests for marker parity.
- T5 (Verification): run sqlite policy/contract-lane and docs-contract test commands.

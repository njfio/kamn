# Tasks — #4246

Status: Reviewed

T1 (Red)
- Add failing fixtures for missing smoke command, leaked run-mode command, workflow leak, and budget overflow.

T2 (Green)
- Implement checker and deterministic reason-taxonomy output surface.

T3 (Regression)
- Add checker test to ci-tools regression suite and verify deterministic fail reasons.

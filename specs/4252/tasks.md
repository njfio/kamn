# Tasks — #4252 Finality Taxonomy and Runbook Parity

Status: Implemented

- T1 (Regression): add Red tests for finality taxonomy drift and runbook marker divergence (`#4257`).
- T2 (Implementation): add deterministic taxonomy/runbook parity markers and fail-closed reason mapping in convergence checker (`#4258`).
- T3 (Integration): enforce marker parity in convergence contract-lane outputs and runbook-file checks.
- T4 (Docs): update strategy, deploy compatibility runbook, and release checklist marker contracts.
- T5 (Verification): run targeted script tests + doc tests + clippy/fmt and map AC/C-cases in PR evidence.

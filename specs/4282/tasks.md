# Tasks — #4282

Status: Implemented

T1 (Tests first)
- Add RED tests for taxonomy drift and runbook marker divergence rejection (`#4287`).

T2
- Implement taxonomy enforcement and runbook parity contract checks in policy checker mode (`#4288`).

T3
- Update release checklist/deploy docs and docs-contract tests for marker parity.

T4
- Run targeted validations, open PR, merge, and close issue chain.

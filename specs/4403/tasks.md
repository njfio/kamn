# Tasks — #4403

Status: Reviewed

T1 (RED)
- Add failing tests for missing required emission payload fields and normalized reason output marker expectations.

T2 (GREEN)
- Implement deterministic missing-field reason mapping and normalized reason output markers in runtime observability endpoint policy checker.

T3 (Regression)
- Re-run runtime observability endpoint policy + contract lane suites and verify deterministic fail-closed behavior remains intact.

T4 (Docs)
- Update `docs/observability/schema.md` with runtime observability emission payload checker matrix and failure taxonomy markers.


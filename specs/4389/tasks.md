# Tasks — #4389

Status: Reviewed

T1 (RED)
- Add failing assertions for persistence tamper/freshness and incomplete evidence acceptance (`#4396`).

T2 (GREEN)
- Implement deterministic persistence gate taxonomy and CI/local boundary enforcement (`#4397`).

T3 (Integration)
- Add parity checks across stdout markers, JSON report fields, and docs contracts.

T4 (Docs)
- Update release checklist and CI strategy persistence governance sections.

T5 (Regression/Gates)
- Run target persistence script suites, docs-contract tests, and repo gates.

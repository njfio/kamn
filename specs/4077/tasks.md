# Issue #4077 Tasks

- T1 (Tests first): add docs-parity assertion and capture RED for missing deletion-proof marker section.
- T2 (Implementation): add deletion-proof fixture matrix and checker behavior contract tests.
- T3 (Docs): add deletion-proof marker map section in `docs/ops/configuration.md`.
- T4 (Verification): run targeted tests + `cargo fmt --check` + `cargo clippy -- -D warnings`.
- T5 (Process): publish issue progress logs and open PR with AC/tier/TDD evidence.

# Issue #3772 Tasks

- Issue: #3772
- Status: In Progress

## Ordered Tasks
- [x] T1 (Implement): complete observability/tracing hardening story (`#3773`).
- [x] T2 (Implement): complete transport retry/reconnect hardening story (`#3774`).
- [x] T3 (Verify): run epic-level combined verification bundle across transport resilience/docs/CI exclusion contracts.
- [ ] T4 (Closeout): merge closure PR and close epic issue with status markers.

## Tier Mapping
- Unit: retry/taxonomy helper contract checks.
- Functional: reconnect pacing and CI exclusion policy behavior checks.
- Integration: contract-lane and docs parity checks.
- Regression: marker/taxonomy drift guard checks.
- Performance: bounded CI/local-heavy governance checks (no new workload introduced in closeout PR).

# Plan — #4270

Status: Reviewed

- Expand axum ingress policy/lane shell tests with deterministic repeated-run mismatch ordering checks.
- Assert mismatch reason mapping markers in both command output and generated policy JSON.

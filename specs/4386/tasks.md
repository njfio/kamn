# Tasks — #4386

Status: Reviewed

T1 (RED)
- Add failing tests for lifecycle limit breach and unstable backpressure outcome cases (`#4390`).

T2 (GREEN)
- Implement deterministic lifecycle rejection reason mapping and normalized policy output (`#4391`).

T3 (Integration)
- Extend service API ingress contract-lane marker coverage and lane-policy parity checks.

T4 (Docs)
- Update `docs/service/api-contract.md` and `docs/ops/configuration.md` for lifecycle/backpressure taxonomy parity.

T5 (Regression)
- Run service API ingress policy and contract-lane suites plus targeted Rust tests for lifecycle reason stability.

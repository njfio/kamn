# Issue #4956 Tasks

- Issue: #4956
- Status: Implemented

## Ordered Tasks
- [x] T1: define archive policy and marker contracts (`#4961`).
- [x] T2: implement archive tooling and placement checker coverage (`#4962`).
- [x] T3: execute first archive migration wave and parity hardening (`#4963`).
- [x] T4: synchronize story lifecycle docs to Implemented.

## Completion Evidence
- Tasks delivered: `#4961` (closed), `#4962` (closed), `#4963` (closed)
- `bash scripts/ci/test_check_spec_archive_policy.sh`

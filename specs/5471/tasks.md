# Issue #5471 Tasks

- T1 (RED): add failing daemon test for runtime row-count vs selector-row alignment.
- T2 (Implementation): add production selector-row source and derive runtime row-count marker from it.
- T3 (Regression): run targeted daemon runtime bundle tests and format checks.
- T4 (Process): update spec status, open PR, and complete closure markers.

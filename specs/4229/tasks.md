# Tasks — #4229

Status: Implemented

T1 (Red)
- Add failing tests for missing evidence link, payload tamper, and reason-mapping tamper fixtures.

T2 (Green)
- Update checker/lane behavior until tests pass with deterministic fail-closed outputs.

T3 (Regression)
- Confirm repeated tamper runs produce stable reason markers/order.

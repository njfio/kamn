# Issue #5420 Tasks

- T1 (Tests first): update extraction/docs contract tests with phase-3 marker and include expectations (red).
- T2 (Implementation): extract runtime and live-postgres matrix slices into dedicated include modules; wire root shell includes.
- T3 (Docs): record phase-3 decomposition markers in ops docs and R45 review ledger.
- T4 (Verification): run targeted daemon/docs tests plus `cargo fmt --check` and `cargo clippy -- -D warnings`.

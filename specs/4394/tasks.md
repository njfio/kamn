# Tasks — #4394

Status: Implemented

T1 (RED)
- Add failing validation + policy assertions for peer reason marker emissions.

T2 (RED)
- Add failing policy tamper assertions for timeout and peer-integrity reason marker mismatch.

T3 (RED)
- Add failing contract-lane assertions for peer docs-parity markers.

T4 (Regression)
- Re-run targeted live transport suites after #4395 implementation.

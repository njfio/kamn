# Tasks: Issue #5887

## Ordered Tasks
1. T1 (Conformance): Capture baseline all-roots panic-path violations.
2. T2 (Implementation): Add explicit env fallback helpers and migrate `sdk_direct.rs` call sites.
3. T3 (Implementation): Add explicit env fallback helpers and migrate `cli_scripted.rs` call sites.
4. T4 (Implementation): Add explicit env fallback helpers and migrate `mcp_agent.rs` call sites.
5. T5 (Implementation): Expand checker default roots to include e2e harness src.
6. T6 (Verification): Run C-01..C-04 command set + e2e harness tests.
7. T7 (PR/Closure): Open PR with AC mapping, TDD evidence, and closure markers.

# Issue #5495 Tasks

- T1 (RED): capture pre-cleanup count and demonstrate target count `50` is not yet met.
- T2 (Candidate Safety): compute stale-safe candidate set (closed issue, no open PR, non-merged).
- T3 (Implementation): delete exactly one candidate branch.
- T4 (GREEN/Verify): verify post-cleanup branch count `50` and capture evidence.
- T5 (Process): open PR with evidence, merge, close issue and milestone.

# Tasks: #4361 Key-Source Reason Mapping

- T1: Implement key-source contract-version checks in summary + contracts object.
- T2: Implement explicit command key-source marker check for real-node profile.
- T3: Emit deterministic key-source taxonomy outputs (`version`, `codes_csv`, `codes_value`).
- T4: Update key-management and release-go/no-go docs with new marker contract.
- T5: Validate targeted script and docs tests.

# Tasks: Issue #5885

## Ordered Tasks
1. T1 (Conformance): Capture baseline shell metrics and identify target files by blank-line density.
2. T2 (Implementation): Compact `scripts/ci` target scripts (whitespace-only).
3. T3 (Implementation): Compact `scripts/runtime` target scripts (whitespace-only).
4. T4 (Implementation): Compact `scripts/kolme`/`scripts/deploy` target scripts (whitespace-only).
5. T5 (Verification): Run C-01..C-06 command set and record outputs.
6. T6 (PR/Closure): Open PR with AC mapping, TDD evidence, tier matrix, and shell-surface markers.

# Tasks — #4387

Status: Implemented

T1 (RED)
- Add failing websocket policy and contract-lane assertions for protocol drift and invalid-session acceptance (`#4392`).

T2 (GREEN)
- Implement deterministic websocket protocol/session reason mapping and normalized policy output (`#4393`).

T3 (Integration)
- Add docs-contract parity checks in websocket live validation and contract lane.

T4 (Docs)
- Update websocket protocol/session taxonomy references in service and release docs.

T5 (Regression)
- Run websocket policy/validation/contract-lane suites and targeted protocol-session Rust tests.

# Issue 6228 Tasks

- T1 (Red/Regression): Add failing tests for CRLF DID route payload and CRLF auth scope payload.
- T2 (Green/Implementation): Ensure existing validators produce deterministic fail-closed errors for new cases.
- T3 (Regression): Run `cargo test -p kamn-sdk service_api_client`.
- T4 (Verification): Map AC/C-cases and close issue with test evidence.

# Issue #5521 Tasks

- T1 (RED): add failing `/metrics` assertions for lifecycle-rejection taxonomy marker lines.
- T2 (Implementation): add canonical runtime lifecycle-rejection taxonomy constants and expose markers in metrics snapshot/payload.
- T3 (GREEN): run targeted unit/integration service API endpoint tests.
- T4 (Regression/Verify): run route-contract regression and formatting checks.
- T5 (Process): open PR with P1 human-review flag, merge, close issue and milestone.

# Tasks — #4285

Status: Implemented

T1 (Tests first)
- Add failing tests for missing live-node drift marker, drift mismatch marker, and deterministic repeated mismatch output.

T2
- Re-run new tests after implementation and preserve as regression coverage.

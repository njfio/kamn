# Tasks — #4276

Status: Implemented

T1: Write fixture-driven RED checks for missing marker and heavy-lane leakage cases.

T2: Implement checker and deterministic reason taxonomy output.

T3: Wire checker and websocket-session smoke commands into CI tools lanes.

T4: Run targeted checker + CI tools fast-mode validation.

# Tasks — #4296

Status: Reviewed

T1 (Tests first)
- Add RED tests for retry-envelope exhaustion and deterministic fail-closed outputs (`#4300`).

T2
- Implement reconnect sequencing and bounded backoff policy enforcement with deterministic reason projection (`#4301`).

T3
- Update ops configuration docs and docs-contract tests for envelope governance markers.

T4
- Execute targeted validations, open PR, merge, and close issue chain.

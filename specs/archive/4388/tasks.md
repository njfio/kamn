# Tasks — #4388

Status: Implemented

T1 (RED)
- Add failing assertions for deterministic peer reason markers and tamper rejection (`#4394`).

T2 (GREEN)
- Implement deterministic peer reason marker checks and policy mismatch mapping (`#4395`).

T3 (Integration)
- Add docs-contract parity checks for peer reason matrix markers.

T4 (Docs)
- Update release checklist and Kolme devnet ops peer reason matrix markers.

T5 (Regression)
- Run live transport validation/policy/contract-lane tests and workspace regression gates.

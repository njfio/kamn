# Tasks — #4269

Status: Implemented

T1 (Tests first)
- Add RED checker fixtures for websocket-session marker drift and heavy session drill leakage (`#4276`).

T2
- Implement deterministic checker logic and CI tools wiring for websocket-session smoke governance (`#4276`).

T3
- Update docs and docs-contract assertions for marker and boundary parity (`#4277`).

T4
- Run targeted validation set, open PR, merge, and close issue chain.

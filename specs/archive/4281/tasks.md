# Tasks — #4281

Status: Implemented

T1 (Tests first)
- Add RED tests for live-node drift marker missing/mismatch rejection and deterministic repeated reason mapping (`#4285`).

T2
- Implement deterministic fail-closed checker policy mode and reason mapping in preflight shared contract module (`#4286`).

T3
- Update ops configuration docs and docs-contract tests for failover drift mismatch policy markers.

T4
- Run targeted validations, open PR, merge, and close issue chain.

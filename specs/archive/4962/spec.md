# Issue #4962 Spec

- Title: Task: implement spec archival tooling and active-vs-archived placement checker
- Status: Implemented
- Type: task
- Priority: P0
- Milestone: specs/milestones/r27-44-shell-loc-deletion-wave-and-hard-ceiling-governance/index.md

## Problem Statement
Policy definitions alone were insufficient; the milestone required deterministic tooling to migrate completed specs and checks that distinguish active vs archived placement.

## Acceptance Criteria
- AC-1: Archive migration tool safely migrates completed issue specs and emits deterministic outputs.
- AC-2: Placement checker/tests fail when active/archived placement contracts drift.
- AC-3: Archive index/pointer artifacts generated by tooling are validated by tests.
- AC-4: Scoped archive tooling and contract tests pass.

## Scope
In scope:
- Deterministic archive migration tool.
- Contract tests for tool outputs and placement-policy behavior.

Out of scope:
- Archive policy definition itself (covered by #4961).
- Initial migration-wave publication and parity hardening (covered by #4963).

## Conformance Cases
| Case | AC | Tier | Input | Expected |
|---|---|---|---|---|
| C-01 | AC-1 | Functional | `scripts/ci/archive_completed_specs.py` dry-run/apply paths | deterministic archive/pointer/index output |
| C-02 | AC-2 | Regression | placement drift fixtures | checker fails closed |
| C-03 | AC-3 | Unit/Regression | tool-generated fixtures in test suite | outputs satisfy policy checks |
| C-04 | AC-4 | Integration | `bash scripts/ci/test_check_spec_archive_policy.sh` | archive tool + placement contracts pass |

## Test Mapping
- AC-1/AC-3: `bash scripts/ci/test_check_spec_archive_policy.sh`
- AC-2: `bash scripts/ci/test_check_spec_archive_policy.sh` drift cases
- AC-4: `bash scripts/ci/test_check_spec_archive_policy.sh`

## Success Metrics
- Deterministic archive migration tool is present and exercised by contract tests.
- Active-vs-archived placement regressions fail closed.

# Tasks — #4393

Status: Implemented

T1 (GREEN)
- Implement deterministic required-field reason mapping and normalized `reason_codes_value` in websocket policy checker.

T2 (GREEN)
- Add release-checklist protocol/session docs parity checks in websocket validation lane and emit parity status markers.

T3 (Integration)
- Update websocket contract-lane marker sets and tests for reason-value and docs parity markers.

T4 (Regression)
- Run websocket policy/validation/contract-lane tests and targeted protocol/session Rust tests.

# Tasks — #4284

Status: Implemented

T1 (Tests first)
- Add RED CI smoke checker tests for failover marker drift and heavy-lane exclusion (`#4291`).

T2
- Implement deterministic checker logic and fail-closed reason mapping for drift/boundary violations (`#4291`).

T3
- Update CI/docs marker contracts and docs-contract tests (`#4292`).

T4
- Run targeted validation set, open PR, merge, and close issue chain.

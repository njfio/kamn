# Issue #5519 Tasks

- T1 (RED): add failing `/metrics` assertions for auth and scope-policy taxonomy markers.
- T2 (Implementation): expose canonical auth/scope taxonomy versions and reason-code counts in runtime metrics payload.
- T3 (GREEN): run targeted unit/integration endpoint tests for `/metrics` marker visibility.
- T4 (Regression/Verify): run additional endpoint regression and formatting checks.
- T5 (Process): open PR with P1 human-review flag, merge, close issue and milestone.

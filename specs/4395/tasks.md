# Tasks — #4395

Status: Implemented

T1 (GREEN)
- Implement deterministic peer reason marker projection in live transport run-lane and policy outputs.

T2 (GREEN)
- Add deterministic mismatch checks for tampered timeout and peer-integrity reason markers.

T3 (Integration)
- Enforce peer reason docs parity markers and expose them in contract-lane output/report.

T4 (Docs)
- Update release checklist and Kolme devnet ops peer reason marker matrix.

T5 (Regression)
- Run targeted live transport tests and workspace regression gates.

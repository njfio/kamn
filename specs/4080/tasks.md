# Issue #4080 Tasks

- T1 (Tests first): add policy checker Rust contract tests + ci_strategy docs assertions and capture RED.
- T2 (Checker): implement checker wrapper, python policy checker, and shell drift test.
- T3 (Docs): add redaction policy checker contract markers to `docs/ci/strategy.md`.
- T4 (Verification): run targeted checker/docs tests + `cargo fmt --check` + `cargo clippy -- -D warnings`.
- T5 (Process): open PR with AC mapping, TDD evidence, and complete tier matrix.

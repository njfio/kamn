# Tasks — #4239

Status: Reviewed

T1 (Red)
- Add failing CI smoke checker tests for missing composition, run-mode leakage, workflow leakage, and budget overflow.

T2 (Green)
- Implement sqlite crash-recovery CI smoke convergence checker and deterministic reason taxonomy.

T3 (Integration)
- Wire sqlite replay-integrity smoke tests into ci-tools fast mode and register checker test in CI tool suite.

T4 (Docs + Regression)
- Update CI strategy + production plan markers and extend docs-contract tests to fail closed on drift.

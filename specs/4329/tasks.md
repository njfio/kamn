# Tasks — #4329

Status: Reviewed

T1 (Tests first)
- Add RED checks for runtime module-boundary parity drift and reason markers (`#4336`).

T2
- Implement checker + policy reason mapping + ci-smoke marker integration (`#4337`).

T3
- Update runtime architecture/CI docs and docs contract tests.

T4
- Execute targeted lanes, open PR, merge, close child issues/task.

# Issue #4032 Tasks

- T1 (Tests first): add runner contract tests and docs marker assertions that fail before implementation.
- T2 (Implementation): add local-heavy deep dependency fixture + runner contract + wrapper registry entry.
- T3 (Docs): add ops configuration marker block for runner schema/profile/commands.
- T4 (Verification): run targeted lane/docs tests plus `cargo fmt --check` and `cargo clippy -p kamn-core --tests -- -D warnings`.
- T5 (Process): open PR with AC mapping + RED/GREEN evidence, then close issue after merge.

# Tasks — #4328

Status: Reviewed

T1 (Tests first)
- Add RED tests for endpoint payload missing-field and schema-drift cases (`#4334`).

T2
- Implement deterministic checker + fail-closed taxonomy integration in endpoint rendering (`#4335`).

T3
- Update docs and docs contract tests for taxonomy + fail-closed markers.

T4
- Run targeted test matrix, format/lint gates, PR/CI/merge, issue closure evidence.

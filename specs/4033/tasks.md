# Issue #4033 Tasks

- T1 (Tests first): add deep-scan policy checker contract tests and docs parity assertions to capture RED.
- T2 (Implementation): add checker script/wrapper/registry and shell regression harness for deterministic policy outcomes.
- T3 (Docs/CI governance): update `docs/ci/strategy.md` markers and wire checker shell test into CI tools fast/full surfaces.
- T4 (Verification): run targeted tests + `cargo fmt --check` + `cargo clippy -p kamn-core --tests -- -D warnings`.
- T5 (Process): open PR with AC mapping/TDD evidence and close #4033 when merged.

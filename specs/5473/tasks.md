# Issue #5473 Tasks

- T1 (RED): require selector-row CSV runtime marker in daemon contract test and capture failing run.
- T2 (Implementation): emit selector-row CSV marker from runtime selector source.
- T3 (Regression): rerun targeted daemon contracts + strict clippy + fmt.
- T4 (Process): mark spec implemented, open PR, merge, and close issue/milestone.

# Tasks — #4266

Status: Implemented

T1 (Tests first)
- Add RED protocol-marker mismatch determinism tests (`#4270`).

T2
- Implement deterministic mismatch reason mapping outputs in the policy checker (`#4271`).

T3
- Integrate mapping markers in lane policy checks and docs parity.

T4
- Run focused validation suite and complete issue closure workflow.

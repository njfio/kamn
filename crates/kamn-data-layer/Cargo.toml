[package]
name = "kamn-data-layer"
version.workspace = true
edition.workspace = true
license = "Apache-2.0"

[lib]
path = "src/lib.rs"

[dependencies]
k256 = { workspace = true, features = ["ecdsa"] }

[lints]
workspace = true

[package]
name = "kamn-data-layer"
version.workspace = true
edition.workspace = true
license = "Apache-2.0"

[lib]
path = "src/lib.rs"

[dependencies]
k256 = { version = "0.13.4", features = ["ecdsa"] }

[lints]
workspace = true
